scalar: <value>
object {
  template: "${ph:name}: ${ph:value} (${ph:birthday format=\"dd.MM.yyyy\"})"
  engine: act
  parameters {
    name: "str:value"
    value: "int:42"
    birthday: "prop[date]:birthday"
  }
}
